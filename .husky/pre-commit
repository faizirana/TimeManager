echo "🔍 Running lint-staged..."

npm run lint:fix
npm run format

if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed — commit aborted."
  exit 1
fi

echo "✅ Pre-commit checks passed."
exit 0